# ==========================================
# User Management Configuration
# ==========================================

# Default shell assigned to new users
DEFAULT_SHELL="/bin/bash"

# The group that grants administrative (sudo) privileges.
# Note: Use "sudo" for Debian/Ubuntu, and "wheel" for RHEL/CentOS.
ADMIN_GROUP="sudo"

# Default base directory for home folders
HOME_BASE="/home"