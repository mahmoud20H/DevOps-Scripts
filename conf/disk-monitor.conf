# ==========================================
# Disk Monitor Configuration
# ==========================================

# The partition to monitor (default is root)
TARGET_PARTITION="/"

# Alert thresholds (in percentages)
WARNING_THRESHOLD=80
CRITICAL_THRESHOLD=90